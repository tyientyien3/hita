<div class="conrainer">
  <div class="row">
    <div class="col-md-6 col-md-offset-3">
      <h1>詳細変更</h1>
      <% if @user.errors.any? %>
      <div class="alert alert-warning">
        <ul>
          <% @user.errors.full_messages.each do |message| %>
          <li><%= message %> error </li>
          <% end %>
        </ul>
      </div>
    </div>
  </div>
  <% end %>
  <%= form_for(@user) do |f| %>
  <div class="row">
    <div class="col-md-3 col-md-offset-1">
      <h4>Image</h4>
      <div class="form-group has-icon">
        <div class="user-edit-image">
          <%= attachment_image_tag @user, :profile_image, :fill, 60, 60, class: "img-circle pull-left profile-thumb", fallback: "no_image.jpg"%>
        </div>
        <label class="control-label sr-only" for="inputEmail">ProfileImage</label>
        <%= f.attachment_field :profile_image, placeholder: "プロフィール画像", class: "user-edit-form" %>
      </div>
    </div>
    <div class="col-md-3 col-md-offset-3">
      <h4>Name</h4>
      <%= f.text_field :username %>
      <h4>在住ステータス</h4>
      <%= f.select :user_status, User.user_statuses.keys.to_a %>
    </div>
  </div>
  <%# ここに国/都市/地域編集をセレクトボックスで実装予定 %>

  <br>
  <br>
  <br>

  <h4>Introduction</h4>
  <%= f.text_area :introduction, size: "20x10", class: "form-control" %>
  <br><br>
  <%= f.submit 'Update User' %>
  <%= link_to '戻る', user_path(@user), class: "btn btn-primary" %>
  <% end %>
  <br>
  <%= link_to "アカウントを削除する",user_path(@user), class: "btn btn-danger", method: :delete, "data-confirm" => "本当にアカウントを削除しますか？" %>
</div>